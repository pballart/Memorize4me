<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorControlNormal">@color/green</item>
        <item name="colorControlActivated">@color/salmonaken</item>
        <item name="colorControlHighlight">@color/yellow</item>
        <item name="android:datePickerStyle">@style/MyApp.CustomDatePicker</item>
    </style>

    <style name="MyApp.CustomDatePicker" parent="@android:style/Widget.DatePicker">
        <item name="android:divider">@color/green</item>
    </style>

</resources>
